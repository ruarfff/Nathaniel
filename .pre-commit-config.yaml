repos:
  - repo: local
    hooks:
      - id: source-format
        name: Project source whitespace
        entry: python3 tools/format_sources.py --check
        language: system
        pass_filenames: false
        files: '\.(gd|tscn|tres|godot|cfg|py)$'
